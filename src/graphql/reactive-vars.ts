import { makeVar } from "@apollo/client";

export const userName = makeVar("PUNHETAA");

export const currentServerId = makeVar("");

export const currentChannelId = makeVar("");

export const currentChannelName = makeVar("");
